import type { Translations } from '../types';

export const translations: Translations<"en"> = {
    "Header": {
        "Todo list": "Todo List",
        //"Log in": "Log in",
        "Log out": "Log out"
    }
};